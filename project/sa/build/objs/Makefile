# 本次构建要生成的exe文件
EXE =

# 本次构建要生成的lib
LIB = libsa.a
LIBDYN = libsa.dll # cygwin .dll  linux .so
LIBSONAME = libsa.so.1

# 本次构建所依赖的头文件路径(公共头文件路径在总的makefile中定义) 
INCLUDE_DIRS = 

# 本次构建所依赖的库 			   
LINK_LIBS = 

# now begin
include $(ROOT)/build/make.rule