sum(iterable[, start])

Sums @start and the items of an @iterable from left to right and returns the total. -
@start defaults to 0. The @iterable¡®s items are normally numbers, and the start value 
is not allowed to be a string.

>>> sum([1,2])
3
>>> sum((1,2,3))
6


